/// One bounded component derived from an approved Work Card path.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ControlledDevelopmentSystemMapComponent {
    pub name: String,
    pub responsibility: String,
}
